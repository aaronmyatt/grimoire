{
  "date": "2026-07-26",
  "constitutionVersion": "1.1.0",
  "stack": "python",
  "skillStackSupport": "none — this /setup skill version only ships TS generators (references/stack-ts.md, scripts/gen_configs.sh); Python machinery below was hand-authored per explicit human choice, not generated by a packaged script",
  "decisions": {
    "stackGap": "hand-author Python machinery now, accepting no drift-audit support for gen_configs.sh-equivalent files until the skill itself is extended",
    "sliceMap": "coarse component-level slices (verbs, exec, adapter, seeds) under src/grim/, not verb-per-slice (human's explicit choice over the finer-grained proposal); shared kernel = src/grim/db.py + src/grim/cli.py",
    "toolchain": "ruff (format+lint) + mypy --strict + pytest/pytest-cov, all uv-run — no global installs required",
    "lintBaseline": "ruff has no bulk-suppression baseline file (unlike ESLint's --suppress-all); future debt will use ruff check --add-noqa or pyproject.toml per-file-ignores instead",
    "typeBaseline": ".claude/mypy-baseline.txt generated the same way stack-ts.md generates its tsc-baseline.txt (pipe the real tool's output), not via an extra mypy-baseline dependency",
    "ci": "deferred by explicit human choice — no git remote exists yet; generate .github/workflows/ci.yml once one does"
  },
  "handEditedAwayFromPackagedTemplate": [
    "CLAUDE.md §5: swapped 'eslint-suppressions.json, tsc-baseline inventories' example names for '.claude/mypy-baseline.txt' — future drift audits of CLAUDE.md will always show this line as diverged from references/constitution.md",
    "fence.sh: swapped the baseline-deny message's 'eslint --prune-suppressions' reference for the mypy-baseline regeneration command — same drift-audit caveat"
  ],
  "openItems": [
    "No CI workflow yet — generate .github/workflows/ci.yml (format/typecheck/lint/test+coverage/boundaries/file-lengths/PR-size) once this repo has a remote",
    "gate.sh's `test` check will fail (pytest exit 5, 'no tests collected') on ANY dirty tree until the first real pytest test file exists — expected pressure to land build plan Phase 0's smoke test early, not a bug",
    "Extend the /setup skill itself with references/stack-python.md + Python-aware gen_configs.sh if more Python projects get set up this way"
  ],
  "postSetupUpdates": [
    {
      "date": "2026-07-26",
      "change": "fence.sh: replaced $TASK_SLICE env-var-based slice gating with git-diff-inferred active-slice detection, matching the packaged /setup skill's scripts/gen_hooks.sh update. No more `export TASK_SLICE=<slice>` — the fence now derives the active slice from `git status --porcelain`: the first write to a slice on a clean tree establishes it, a write to a different slice is denied until that slice's changes are committed (or discarded), and a tree already dirty across >1 slice is denied outright until resolved.",
      "howApplied": "Hand-patched only the slice_of/active_slices functions and the slice-gating block, NOT a full gen_hooks.sh regenerate-and-overwrite — a blanket regenerate would have reverted this repo's Python-specific customizations (the mypy-baseline deny message in fence.sh, and feedback.sh/gate.sh entirely, which are hand-authored per skillStackSupport above and have no TS-flavored equivalent to regenerate from).",
      "verified": "tested directly against this repo's real slice layout (src/grim/{verbs,exec,adapter,seeds}) before landing: clean-tree write allowed, same-slice write allowed after dirtying verbs/, cross-slice write to exec/ denied while verbs/ was dirty, tree restored to clean and re-verified."
    },
    {
      "date": "2026-07-26",
      "change": "Added .pi/extensions/guardrails/index.ts — a Pi-native port of fence.sh/feedback.sh/gate.sh, generated by the /setup skill's new scripts/gen_pi_extension.sh against pi.on(\"tool_call\"/\"tool_result\"/\"agent_settled\") instead of Claude Code's PreToolUse/PostToolUse/Stop hooks. Same enforcement (frozen-path ask via ctx.ui.confirm, fails closed to deny when Pi has no UI to confirm through; baseline-path deny; append-only ledgers; git-diff-inferred slice gating; format/typecheck/lint/test gate forcing another turn via pi.sendUserMessage). Also added '.pi/extensions/**' to budgets.json.slices.frozenPaths (the Pi-harness equivalent of '.claude/**') and regenerated fence.sh via gen_hooks.sh to pick it up.",
      "howApplied": "gen_pi_extension.sh generated the extension fresh from this repo's real budgets.json (no packaged Python template needed — the generator is stack-agnostic, unlike gen_configs.sh); hand-reapplied the same mypy-specific baseline-deny wording fence.sh already carries, for consistency between the two harnesses. fence.sh was regenerated via gen_hooks.sh (confirmed stack-agnostic — the diff before reapplying the known customization was exactly the new frozen pattern plus the one documented message swap, nothing else) rather than hand-patched, since this generator doesn't depend on the TS-specific gap that blocks gen_configs.sh. gate.sh's regenerated output matched the live file byte-for-byte (already fully gen_hooks.sh-compatible). feedback.sh untouched (hand-authored, no Pi equivalent needed — Pi's PostToolUse-equivalent is the new extension's tool_result handler).",
      "verified": "node --experimental-strip-types --check passed on the generated extension (dependency-free syntax check). Behavioral proof via scripts/pi_canary_driver.ts confirmed frozen-path writes are denied fail-closed when @earendil-works/pi-coding-agent is resolvable (tested by pointing NODE_PATH... note: NODE_PATH doesn't apply to ESM resolution, so this repo's own canary run correctly deferred the behavioral half to a live Pi session — see scripts/canary.sh's own graceful-degradation path, which reported PASS on the syntax check and named the live-session step as the remaining proof). fence.sh's frozen-path ask verified directly against .pi/extensions/foo.ts post-regenerate. drift_audit.sh run after landing: only pre-existing documented divergences plus the two Pi-related additions show as drift, nothing unexpected."
    }
  ]
}
